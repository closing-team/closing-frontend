export default function LoginPage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-[#F8F8F9] px-6">
      <section className="flex w-full max-w-[375px] flex-col items-center gap-4 rounded-2xl bg-white px-6 py-10 text-center shadow-sm">
        <h1 className="text-2xl font-bold text-[#6558FF]">Closing</h1>
        <p className="text-sm leading-relaxed text-gray-500">
          로그인 화면은 P1에서 카카오 로그인 플로우와 함께 구현합니다.
        </p>
      </section>
    </main>
  );
}
