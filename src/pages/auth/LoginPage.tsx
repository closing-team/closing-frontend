export default function LoginPage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-gray-30 px-6">
      <section className="flex w-full max-w-app flex-col items-center gap-4 rounded-2xl bg-white px-6 py-10 text-center shadow-sm">
        <h1 className="text-head-1 text-primary-500">Closing</h1>
        <p className="text-body-2 text-gray-500">
          로그인 화면은 P1에서 카카오 로그인 플로우와 함께 구현합니다.
        </p>
      </section>
    </main>
  );
}
